# 03 - Python AI 大脑层

> 这是宠物的"大脑"，负责理解用户的话、调用工具、记住事情、做出回应。所有智能相关的逻辑都在这一层。

---

## 3.1 服务框架模块

### 3.1.1 搭建 FastAPI WebSocket 服务器
- [ ] 创建 `server.py`
- [ ] 使用 FastAPI 创建 WebSocket 端点（`@app.websocket("/ws")`）
- [ ] 实现连接建立和断开处理
- [ ] 验证：Unity 能成功连接

### 3.1.2 实现请求路由分发
- [ ] 创建注册表模式（`dict` 存储 method → handler 映射）
- [ ] 根据消息中的 `method` 字段分发到对应处理器
- [ ] 新增方法时只需注册，无需修改核心代码
- [ ] 验证：`chat`、`action`、`config` 等方法能正确路由

### 3.1.3 实现连接池管理
- [ ] 维护已连接客户端列表
- [ ] 监听连接断开事件并清理
- [ ] 实现向所有客户端广播消息的能力
- [ ] 验证：多客户端连接时管理正常

### 3.1.4 实现配置加载（YAML + .env）
- [ ] 使用 `PyYAML` 加载 `config.yaml`
- [ ] 使用 `python-dotenv` 加载 `.env`
- [ ] 实现配置热加载（修改文件后无需重启）
- [ ] 验证：配置变更即时生效

### 3.1.5 集成 Loguru 日志系统
- [ ] 安装并配置 `loguru`
- [ ] 输出到控制台和文件
- [ ] 结构化日志格式（时间 / 级别 / 模块 / 内容）
- [ ] 验证：日志格式规范，便于排查问题

### 3.1.6 实现服务生命周期管理
- [ ] 使用 `asyncio` 管理启动和停止
- [ ] 处理 SIGINT / SIGTERM 信号实现优雅关闭
- [ ] 关闭前清理资源（关闭数据库连接、释放内存）
- [ ] 验证：按 Ctrl+C 能优雅退出

### 3.1.7 实现健康检查端点
- [ ] 创建 `/health` HTTP GET 端点
- [ ] 返回服务状态（运行中 / 异常）
- [ ] 验证：浏览器访问 `localhost:8000/health` 返回正常

---

## 3.2 LLM 集成模块

### 3.2.1 实现统一 LLM 客户端
- [ ] 创建 `llm/client.py`
- [ ] 封装 OpenAI SDK 的调用接口
- [ ] 通过 `base_url` + `api_key` 实现服务切换
- [ ] 验证：配置变更即可切换不同服务商

### 3.2.2 实现多模型配置管理
- [ ] 在 `config.yaml` 中定义 `models` 列表
- [ ] 支持主模型 + 备用模型 + 本地模型的配置
- [ ] 运行时读取配置并初始化客户端
- [ ] 验证：配置中的多个模型都能被识别

### 3.2.3 接入 OpenAI 兼容 API
- [ ] 配置 OpenAI 官方端点（或其他兼容端点）
- [ ] 实现标准对话请求（`chat.completions.create`）
- [ ] 验证：能正常获取回复

### 3.2.4 接入本地 Ollama 模型
- [ ] 配置 `base_url=http://localhost:11434/v1`
- [ ] 测试本地模型（如 qwen2.5:7b）
- [ ] 验证：断网环境下仍能运行

### 3.2.5 接入国内模型
- [ ] 接入 DeepSeek API
- [ ] 接入智谱 GLM API
- [ ] 接入通义千问 API
- [ ] 验证：国内模型能正常返回中文回复

### 3.2.6 实现流式响应
- [ ] 使用 `AsyncGenerator` 逐 Token 返回
- [ ] Unity 端接收流式数据并实时显示
- [ ] 验证：实现打字机效果，文字逐字出现

### 3.2.7 实现上下文管理
- [ ] 使用 `List[Dict]` 维护消息历史
- [ ] 实现多轮对话上下文传递
- [ ] 控制上下文长度避免 Token 超限
- [ ] 验证：多轮对话有连贯性

### 3.2.8 实现 Token 计数
- [ ] 集成 `tiktoken` 库
- [ ] 统计每次请求的 Token 消耗
- [ ] 输出到日志供用户查看
- [ ] 验证：Token 计数准确

### 3.2.9 实现降级策略（主模型异常切换备用）
- [ ] 主模型请求失败时自动切换到备用模型
- [ ] 备用也失败时切换到本地模型
- [ ] 任意模型可用即可返回结果
- [ ] 验证：故意填错主模型 Key，看是否能用备用模型回复

### 3.2.10 实现重试机制
- [ ] 使用 `tenacity.retry` 实现指数退避重试
- [ ] 参数：初始 1s，最大 30s，最多 3 次
- [ ] 验证：临时网络故障后自动恢复

---

## 3.3 Agent 核心模块

### 3.3.1 实现 LangChain ReAct 推理引擎
- [ ] 安装 `langchain` 相关包
- [ ] 创建 `agent/core.py`
- [ ] 实现 Thought → Action → Observation 循环
- [ ] 验证：Agent 能自动选择工具完成指令

### 3.3.2 实现 ToolExecutor 工具执行
- [ ] 创建 `tools/base.py` 定义工具基类
- [ ] 实现工具注册和动态调用
- [ ] 工具调用成功率目标 > 95%
- [ ] 验证：Agent 调用工具时返回正确结果

### 3.3.3 实现性格配置（PromptTemplate）
- [ ] 创建 `agent/prompt_templates.py`
- [ ] 设计 System Prompt 模板
- [ ] 支持通过配置切换不同性格
- [ ] 验证：同一模型可输出不同风格回复

### 3.3.4 设计角色设定 System Prompt
- [ ] 确定宠物名字、性格、说话风格
- [ ] 编写核心 System Prompt（如"你是一只可爱的电子宠物..."）
- [ ] 在每次对话时注入 System Prompt
- [ ] 验证：回复语气符合设定

### 3.3.5 实现对话记忆（ConversationBufferMemory）
- [ ] 集成 LangChain 的 `ConversationBufferMemory`
- [ ] 保留最近 N 轮对话上下文
- [ ] 验证：5 轮内对话有连贯性

### 3.3.6 实现意图识别
- [ ] 使用 LLM 推理判断用户意图
- [ ] 辅助关键词匹配提高准确率
- [ ] 分类：闲聊 / 指令 / 查询
- [ ] 验证："今天天气"被识别为查询，"你好"被识别为闲聊

### 3.3.7 实现任务规划（Plan-and-Execute）
- [ ] 复杂任务自动拆分为子任务
- [ ] 按顺序执行子任务
- [ ] 汇总结果返回给用户
- [ ] 验证："安排明天日程"自动拆分为查询日历 + 添加提醒

---

## 3.4 工具集

### 3.4.1 实现工具基类与注册机制
- [ ] 定义工具基类（`BaseTool`）
- [ ] 实现工具注册装饰器或函数
- [ ] Agent 自动发现已注册工具
- [ ] 验证：新增工具自动可用

### 3.4.2 实现天气查询工具
- [ ] 接入免费天气 API（如 wttr.in）
- [ ] 输入：`city: str`
- [ ] 输出：温度 / 天气状况 / 湿度
- [ ] 验证："北京天气"返回正确信息

### 3.4.3 实现时间查询工具
- [ ] 使用 `datetime` 模块
- [ ] 支持时区参数（可选）
- [ ] 输出格式化时间字符串
- [ ] 验证：返回当前时间正确

### 3.4.4 实现屏幕截图工具
- [ ] 使用 `mss` 库捕获屏幕
- [ ] 支持指定区域截图（可选参数）
- [ ] 输出图像数据（PIL Image 或 bytes）
- [ ] 验证：截图内容与屏幕一致

### 3.4.5 实现屏幕分析工具（VLM / OCR）
- [ ] 截图后调用 VLM（如 GPT-4V）或本地 OCR
- [ ] 输入：`query: str`（如"这是什么图标"）
- [ ] 输出图像理解结果
- [ ] 验证：能正确描述屏幕内容

### 3.4.6 实现数学计算工具
- [ ] 使用安全 `eval`（限制操作符）或 `operator` 模块
- [ ] 输入：`expression: str`
- [ ] 输出计算结果
- [ ] 验证："1+2*3"返回 7

### 3.4.7 实现系统信息查询工具
- [ ] 使用 `psutil` 获取 CPU / 内存 / 磁盘信息
- [ ] 输入：`info_type: str`
- [ ] 输出系统状态
- [ ] 验证：返回的信息与任务管理器一致

### 3.4.8 实现网络搜索工具
- [ ] 接入搜索 API（如 Tavily / Bing Search）
- [ ] 输入：`query: str`
- [ ] 输出搜索结果摘要
- [ ] 验证：搜索关键词返回相关结果

### 3.4.9 实现定时提醒工具
- [ ] 使用 `sched` + `threading` 实现定时器
- [ ] 输入：`time: str`, `message: str`
- [ ] 到时间后主动推送给 Unity
- [ ] 验证：设置的提醒准时触发

### 3.4.10 实现启动应用工具
- [ ] 使用 `subprocess.Popen` 启动程序
- [ ] 输入：`app_name: str`
- [ ] 输出启动结果
- [ ] 验证：能正确打开记事本 / 浏览器等

---

## 3.5 记忆系统

### 3.5.1 实现短期记忆（滑动窗口）
- [ ] 使用 `List[Dict]` 保留最近 20 条对话
- [ ] 实现窗口滑动逻辑（超限时丢弃最旧记录）
- [ ] 验证：窗口内对话完整可回溯

### 3.5.2 实现长期记忆（SQLite 持久化）
- [ ] 创建 `memory.db` 数据库
- [ ] 设计表结构：`id, type, content, timestamp, importance`
- [ ] 实现增删改查接口
- [ ] 验证：重启服务后记忆可恢复

### 3.5.3 实现用户偏好存储
- [ ] 存储用户姓名、喜好、习惯等
- [ ] 使用键值对或 JSON 格式
- [ ] 验证："我叫小明"后续对话能正确称呼

### 3.5.4 实现情感记忆记录
- [ ] 检测对话中的情绪标签
- [ ] 记录情绪事件 + 时间戳
- [ ] 验证：可查询"上次开心是什么时候"

### 3.5.5 实现摘要压缩
- [ ] 每 50 条对话触发一次摘要
- [ ] 使用 LLM 将长对话压缩为摘要
- [ ] 保留关键信息，节省 Token
- [ ] 验证：摘要能覆盖主要内容

### 3.5.6 实现记忆检索
- [ ] 使用 SQL `LIKE` 进行关键词匹配
- [ ] 支持全文搜索（SQLite FTS 扩展）
- [ ] 验证："我之前说过什么"返回相关记录

### 3.5.7 实现重要性评估与遗忘
- [ ] 设计简单遗忘曲线算法（访问频率 + 最近访问时间）
- [ ] 定期清理低重要性记忆
- [ ] 长期记忆上限 1000 条
- [ ] 验证：记忆数量不会无限增长

---

## 3.6 感知模块

### 3.6.1 集成屏幕 OCR（PaddleOCR / Tesseract）
- [ ] 安装 PaddleOCR 或 Tesseract
- [ ] 截图后提取屏幕上的文字
- [ ] 验证：能识别桌面图标文字 / 弹窗内容

### 3.6.2 集成图像识别（CLIP / BLIP）
- [ ] 安装 CLIP 或 BLIP 模型
- [ ] 对屏幕截图进行内容理解
- [ ] 验证："这是什么图标"能正确描述

### 3.6.3 实现窗口监测
- [ ] 使用 `pygetwindow` 获取当前活动窗口
- [ ] 记录窗口标题和进程名
- [ ] 验证：可回答"你现在在看什么"

### 3.6.4 实现用户活动检测（空闲检测）
- [ ] 使用 `pynput` 监听键盘和鼠标
- [ ] 计算用户空闲时间
- [ ] 闲置超过 5 分钟通知 Unity 进入休眠
- [ ] 验证：长时间不操作宠物进入睡眠
